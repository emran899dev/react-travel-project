import React from 'react';

const Destination = () => {
    return (
        <div style={{marginTop: '50px'}}>
            <h1>Destination Page....</h1>
        </div>
    );
};

export default Destination;